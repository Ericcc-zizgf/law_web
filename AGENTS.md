# Project workflow

- After each completed and verified project change, create a focused local Git commit automatically.
- Stage only files that belong to the requested change; preserve unrelated and untracked work.
- Do not push commits to a remote unless the user explicitly asks to push.
